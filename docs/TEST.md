# Test description

This section provides details on how to run the tests for the 99minutos application.

## Run integration tests

The integration tests are described with **Gherkin Syntax** in the path `test/features`.

You can run the integration tests using the following command:

```sh
go test -tags=integration ./test/bdd
```
