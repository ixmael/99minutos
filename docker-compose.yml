services:
  repository:
    image: docker.io/library/postgres:18.2-alpine3.23
    container_name: 99minutos_repository
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: 99minutos
    ports:
      - "5432:5432"

# networks:
#   services-nt:
#     driver: bridge
